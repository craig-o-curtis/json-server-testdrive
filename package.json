{
  "name": "json-server-testdrive",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "json-server -p 3333 --watch ./db.json",
    "faker": "json-server -p 3333 --watch ./generate.js",
    "module": "node ./existing-project/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "^5.4.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "json-server": "^0.16.3"
  }
}
